Minimal Hugo site
=================

 - Few pages:
    - Home page;
    - Few posts;
    - Few 'regular' pages.
 - Minimal set of layouts.
 - One SASS stylesheet.
 - Minimal config.

How to build it
---------------

Just run `hugo` in project folder.
